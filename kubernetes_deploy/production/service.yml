apiVersion: v1
kind: Service
metadata:
  name: laa-cla-public
  namespace: laa-cla-public-production
spec:
  ports:
  - port: 80
    name: http
    targetPort: 80
  selector:
    app: laa-cla-public-app
